# Optional. The plugin sends the user's own key with every request; this is only a fallback.
ANTHROPIC_API_KEY=

# Optional. Override the model without a code change (defaults to claude-sonnet-5).
CLAUDE_MODEL=

# Local dev port (default 3001)
PORT=3001
